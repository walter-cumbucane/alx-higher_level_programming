-- uses the count MySQL function to count the number of rows that
-- returns from a SQL Query
-- Syntax: SELECT COUNT(*) FROM table_name WHERE condition;

SELECT COUNT(*) FROM first_table WHERE id = 89;
